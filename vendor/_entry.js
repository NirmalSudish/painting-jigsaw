// Entry point for `npm run bundle:sdk` — bundles the npm SDK into discord-sdk.js
// so the app can import it locally with no CDN. Regenerate after updating the SDK.
export * from "@discord/embedded-app-sdk";
