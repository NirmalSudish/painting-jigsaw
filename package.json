{
  "name": "painting-jigsaw",
  "version": "1.0.0",
  "description": "Multiplayer jigsaw of famous paintings — web app + Discord Activity.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "serve": "node server.js",
    "bundle:sdk": "esbuild vendor/_entry.js --bundle --format=esm --platform=browser --outfile=vendor/discord-sdk.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "esbuild": "^0.28.0"
  }
}
